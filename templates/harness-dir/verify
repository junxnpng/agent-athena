#!/bin/sh
# 도메인 계약 ② — 전체 검증기 (레벨 0: exit 0 = pass).
# 첫 밤은 `echo ok` 로 시작해도 된다. 진짜 검증기로 바꾸는 순간부터 하네스가 의미를 가진다.
# 레벨 1(스칼라 임계값)은 plan.json 작업의 verify 를 {"cmd": "...", "metric": {"min": 0.9}} 로 쓴다 — 출력 마지막 줄이 숫자.
cd "$(dirname "$0")/.." || exit 1
echo ok
