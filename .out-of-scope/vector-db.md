# 벡터 DB / 그래프 인덱스

- **요청** — 코드·문서 검색을 위한 임베딩 인덱스 또는 지식 그래프
- **이유** — grep + 파일이 기본값이고 아직 병목이 아니다. 인덱스는 갱신 상태라는 두 번째 진실을 만든다 (I3 위반 방향)
- **탈출구** — 관계 질의("이 함수를 바꾸면 어디가 깨지나")가 밤마다 반복되는 것이 로그에서 확인되면 재검토. 그때도 repo 안 파일로 재생성 가능한 형태만
- **과거 요청** — 2026-08-27 · spec v0 §12
