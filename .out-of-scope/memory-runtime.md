# 메모리 런타임 (mem0 / Letta 등)

- **요청** — 밤 간 기억을 위한 외부 메모리 서비스
- **이유** — 파일로 시작한다. `log.jsonl` + git이 이미 밤 간 인수인계이고, `git diff`가 되는 것 자체가 디버깅 도구다. 런타임은 상태를 repo 밖으로 빼내 I3·I8을 흐린다
- **탈출구** — 로그가 너무 길어 컨텍스트에 못 넣는 시점이 오면, 먼저 로그 → 요약 파일 파생(`runner/summary` 확장). 외부 런타임은 그 다음
- **과거 요청** — 2026-08-27 · spec v0 §12
